import { Container } from './style';
import Button from '../../Generic/Button';

export const Root = () => {
  return (
    <Container>
      <h1>Home Component</h1>
      <Button>Login</Button>
    </Container>
  );
};

export default Root;
